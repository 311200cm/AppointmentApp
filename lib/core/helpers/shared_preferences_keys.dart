class SharedPreferencesKeys{
  //TODO: this is name of key in cash that token sorted in by means of if we need token we get it by this key
  static const String userToke="userToken";
}

 bool isLogged=false;